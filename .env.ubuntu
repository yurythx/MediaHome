# Configurações de Ambiente - Stack MediaHome
# Ubuntu 24.04 + aaPanel
# Substitua os valores conforme sua configuração

# ===========================================
# CONFIGURAÇÕES GERAIS
# ===========================================
# IP do servidor Ubuntu (ajustar conforme necessário)
HOST_IP=192.168.0.121

# Timezone
TZ=America/Sao_Paulo

# Usuário e grupo para containers
PUID=1000
PGID=1000

# ===========================================
# CONFIGURAÇÕES JELLYFIN
# ===========================================
# Jellyfin usa as variáveis PUID, PGID e TZ definidas acima
# Portas e volumes são definidos no docker-compose

# ===========================================
# CONFIGURAÇÕES KOMGA
# ===========================================
# Komga usa configuração padrão
# Dados armazenados em: /mnt/config/komga

# ===========================================
# CONFIGURAÇÕES NAVIDROME
# ===========================================
# Navidrome usa configuração padrão
# Dados armazenados em: /mnt/config/navidrome

# ===========================================
# CONFIGURAÇÕES SAMBA
# ===========================================
# Usuário e senha para acesso Samba
SAMBA_USER=suporte
SAMBA_PASSWORD=suporte123

# ===========================================
# CONFIGURAÇÕES DE VOLUMES
# ===========================================
# Caminhos dos volumes no host Ubuntu
CONFIG_PATH=/mnt/config
MEDIA_PATH_1=/mnt/dados
MEDIA_PATH_2=/mnt/dados2

# ===========================================
# CONFIGURAÇÕES DE REDE
# ===========================================
# Rede Docker para comunicação entre serviços
DOCKER_NETWORK=mediahome

# ===========================================
# CONFIGURAÇÕES DE PORTAS
# ===========================================
# Portas expostas no host
JELLYFIN_PORT=8096
KOMGA_PORT=8080
NAVIDROME_PORT=4533
SAMBA_PORT=1445
PORTAINER_PORT=9000

# ===========================================
# CONFIGURAÇÕES DE DOMÍNIOS (aaPanel)
# ===========================================
# Configure estes domínios no aaPanel com proxy reverso
# jellyfin.seudominio.com → 192.168.0.121:8096
# komga.seudominio.com → 192.168.0.121:8080
# navidrome.seudominio.com → 192.168.0.121:4533
# portainer.seudominio.com → 192.168.0.121:9000

# ===========================================
# INSTRUÇÕES DE USO
# ===========================================
# 1. Crie a estrutura de diretórios:
#    sudo mkdir -p /mnt/dados/{filmes,series,musicas,quadrinhos}
#    sudo mkdir -p /mnt/dados2/{filmes,series,musicas,quadrinhos}
#    sudo mkdir -p /mnt/config/{jellyfin,komga,navidrome}
#
# 2. Configure permissões:
#    sudo chown -R 1000:1000 /mnt/config /mnt/dados /mnt/dados2
#    sudo chmod -R 755 /mnt/config /mnt/dados /mnt/dados2
#
# 3. Configure domínios no aaPanel com proxy reverso
#
# 4. Configure SSL para todos os domínios
#
# 5. Execute: docker-compose up -d

# ===========================================
# CONFIGURAÇÕES FINAIS NECESSÁRIAS
# ===========================================
# Após o deploy, configure:
# 1. Jellyfin: Complete wizard e configure bibliotecas
# 2. Komga: Crie conta admin e configure bibliotecas
# 3. Navidrome: Crie conta admin
# 4. Samba: Teste conectividade com credenciais
# 5. Portainer: Crie conta de administrador

# ===========================================
# ESTRUTURA DE MÍDIA RECOMENDADA
# ===========================================
# /mnt/dados/
# ├── filmes/
# │   └── Nome do Filme (Ano)/
# │       └── arquivo.mkv
# ├── series/
# │   └── Nome da Serie/
# │       └── Season XX/
# │           └── episodios.mkv
# ├── musicas/
# │   └── Artista/
# │       └── Album/
# │           └── faixas.mp3
# └── quadrinhos/
#     └── Editora/
#         └── Serie/
#             └── arquivos.cbz